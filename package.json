{
  "name": "dbhelper",
  "version": "0.0.1",
  "description": "Helper library for putting and getting mongodb documents",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "mongodb": "^2.1.14"
  }
}
